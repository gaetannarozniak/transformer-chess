# transformer-chess

## Setup
**Training:**
```bash
cd training
uv sync
```

**Web app:**
```bash
cd app
uv sync
```

## Running
**Web app:**
```bash
cd app
python main.py --stockfish-elo 1350
```

The app runs on `http://localhost:8000` by default.
